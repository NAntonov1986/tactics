/* unit-types.js — справочник типов существ.
   Что внутри:
     • UNIT_TYPES — ключи: living / mechanism / undead / demon / elemental.
       На каждом ключе только { label }. Тип хранится на КЛАССЕ
       (CLASSES[classId].unitType), а не на инстансе юнита, до тех пор
       пока не появятся механики превращения типа (полиморф, обращение
       в нежить и т.п.).
   Что НЕ внутри:
     • Сами правила «иммунитет к яду у нежити», «святой ×1.5 по нежити» —
       они в src/core/damage.js (когда будет извлечён) и в apply-хелперах
       конкретных эффектов в src/core/effects.js.
     • Хелпер isImmuneToPoison(target) — пока остаётся в монолите, переедет
       в src/core/damage.js одновременно с computeIncomingDamage.
   Где править подпись типа: тут.
   Где добавить новый тип существ: добавить запись в UNIT_TYPES + указать
     этот ключ в `unitType` соответствующих классов.
   Файл подключается ОБЫЧНЫМ <script src="..."> до inline-блока в index.html.
   UNIT_TYPES попадает в глобальный scope window.
*/

/* Типы существ. Пока справочные — задел под будущие условные эффекты
   (иммунитеты, уязвимости, модификаторы урона по типу цели). Каждый
   класс в CLASSES обязан указать unitType — ключ из этого реестра. */
const UNIT_TYPES = {
  living:    { label: 'живое существо' },
  mechanism: { label: 'механизм' },
  undead:    { label: 'нежить' },
  demon:     { label: 'демон' },
  elemental: { label: 'элементаль' }
};
